.row
  .col-md-7
    = form_for(resource, as: resource_name,
                         url: registration_path(resource_name),
                         html: { class: ['form-horizontal', 'edit-user-form'] }) do |f|
      = render 'form', f: f, edit: false