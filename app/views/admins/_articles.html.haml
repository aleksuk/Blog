%table.table.table-hover.articles-list
  %thead
    %tr
      %th= t('admins.users.id')
      %th= t('admins.articles.title')
  %tbody
    - @articles.each do |article|
      = render 'admins/article', article: article